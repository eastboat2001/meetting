# Offline Icons

The SVG icons in this folder are downloaded from [Lucide](https://lucide.dev/).

Lucide icons are released under the ISC License. The page embeds the selected
icons as an inline SVG sprite so the interface works without a CDN or network
request at runtime.
